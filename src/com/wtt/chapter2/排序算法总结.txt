
快速排序算法是通用算法中最快的

Java.util.Arrays中的sort方法针对基础数据类型使用的是三向切分的快速排序算法，
而针对引用类型则使用稳定性更好的归并排序。(jdk8中已经无法验证。。。）

稳定性指重复元素的相对位置在排序前后是否改变。
如：一个由事件对象构成的数组，事件是按发生时间插入到数组中的，此时若想按发生地点进行重新排列，但每个地点下事件发生的时间仍然是有序的，
此时，若仍有序，则说明该排序算法是稳定的排序算法。

其中，插入排序，归并排序都是稳定算法
选择排序，希尔排序，快速排序和堆排序都是不稳定算法

